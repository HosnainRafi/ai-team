{
  "files": [
    {
      "path": "temp-convert.js",
      "content": "#!/usr/bin/env node\n// TempConvert CLI\n// Lightweight Node.js CLI for converting temperatures between Celsius and Fahrenheit.\n// Uses readline/promises (Node 17+/20+ recommended). No external dependencies.\n\nimport readline from 'readline/promises';\nimport { stdin as input, stdout as output } from 'process';\n\n// ------------------------- Utility functions ------------------------- //\n\n/**\n * Convert Celsius to Fahrenheit.\n * Formula: (°C × 9/5) + 32\n */\nexport function celsiusToFahrenheit(celsius) {\n  return celsius * 9 / 5 + 32;\n}\n\n/**\n * Convert Fahrenheit to Celsius.\n * Formula: (°F − 32) × 5/9\n */\nexport function fahrenheitToCelsius(fahrenheit) {\n  return (fahrenheit - 32) * 5 / 9;\n}\n\n/**\n * Validate numeric input string and return whether it is a finite number.\n * Accepts scientific notation (e.g. 1e3), leading/trailing whitespace, +/-.\n * Rejects Infinity, NaN, empty string, and non-numeric characters.\n */\nexport function isValidNumberInput(str) {\n  if (typeof str !== 'string') return false;\n  const trimmed = str.trim();\n  if (trimmed === '') return false;\n  // Convert using Number and ensure it's finite\n  const num = Number(trimmed);\n  return Number.isFinite(num);\n}\n\n/**\n * Format a number for display: up to 4 decimal places, removes trailing zeros.\n * Examples: 2 -> \"2\", 2.5 -> \"2.5\", 2.333333 -> \"2.3333\"\n */\nexport function formatNumber(n) {\n  if (!Number.isFinite(n)) return String(n);\n  // Use 4 decimal places max for clean output\n  const s = n.toFixed(4);\n  // Remove trailing zeros and possible trailing dot\n  return s.replace(/(?:\\.0+|(?<=\\.\\d*[1-9])0+)$/, '').replace(/\\.$/, '');\n}\n\n// ------------------------- CLI Presentation Helpers ------------------------- //\n// Use simple ANSI colors for a pleasant CLI without external deps.\nconst ANSI = {\n  reset: '\\u001B[0m',\n  bright: '\\u001B[1m',\n  dim: '\\u001B[2m',\n  red: '\\u001B[31m',\n  green: '\\u001B[32m',\n  yellow: '\\u001B[33m',\n  cyan: '\\u001B[36m',\n};\n\nfunction color(text, code) {\n  return code + text + ANSI.reset;\n}\n\nfunction clearLine() {\n  // Move to new line - keep simple to avoid platform issues\n  return '\\n';\n}\n\n// ------------------------- Interactive Prompts ------------------------- //\n\n/**\n * Ask repeatedly for a numeric value until valid, or until user requests back/exit.\n * Returns an object: { canceled: boolean, exit: boolean, value: number|null }\n * - canceled: user chose to go back to menu\n * - exit: user chose to exit the app\n */\nasync function askForNumber(rl, promptText) {\n  while (true) {\n    let answer;\n    try {\n      answer = await rl.question(promptText);\n    } catch (err) {\n      // If input stream closed (Ctrl+D) or other error, treat as exit\n      return { canceled: false, exit: true, value: null };\n    }\n\n    const trimmed = answer.trim().toLowerCase();\n    if (trimmed === 'b' || trimmed === 'back' || trimmed === 'menu') {\n      return { canceled: true, exit: false, value: null };\n    }\n    if (trimmed === 'q' || trimmed === 'quit' || trimmed === 'exit') {\n      return { canceled: false, exit: true, value: null };\n    }\n\n    if (!isValidNumberInput(answer)) {\n      console.log(color('Invalid number. Please enter a finite numeric value (e.g. 21, -4.5, 1e3).', ANSI.red));\n      continue;\n    }\n\n    return { canceled: false, exit: false, value: Number(answer) };\n  }\n}\n\n// ------------------------- Main Application Loop ------------------------- //\n\nasync function mainLoop() {\n  const rl = readline.createInterface({ input, output, terminal: true });\n\n  // Handle Ctrl+C gracefully\n  const sigintListener = () => {\n    // Move to a newline and close\n    console.log(clearLine() + color('Received interrupt. Exiting...', ANSI.yellow));\n    rl.close();\n    process.exit(0);\n  };\n  process.on('SIGINT', sigintListener);\n\n  try {\n    console.log(color('TempConvert CLI — Temperature converter (Celsius <-> Fahrenheit)', ANSI.cyan));\n    console.log(color('Type the option number and press Enter. At any prompt, type b/back to return to the menu, or q/exit to quit.', ANSI.dim));\n\n    mainLoop: while (true) {\n      console.log();\n      console.log(color('Menu:', ANSI.bright));\n      console.log('  1) Convert Celsius → Fahrenheit');\n      console.log('  2) Convert Fahrenheit → Celsius');\n      console.log('  3) Show formulas');\n      console.log('  4) Exit');\n\n      let choice;\n      try {\n        choice = await rl.question(color('Select an option (1-4): ', ANSI.yellow));\n      } catch (err) {\n        console.log();\n        console.log(color('Input closed. Exiting...', ANSI.yellow));\n        break;\n      }\n\n      const c = choice.trim();\n      if (c === '4' || c.toLowerCase() === 'q' || c.toLowerCase() === 'exit') {\n        console.log(color('Goodbye!', ANSI.green));\n        break;\n      }\n\n      if (c === '1') {\n        // Celsius -> Fahrenheit\n        const res = await askForNumber(rl, color('Enter temperature in Celsius (e.g. 25): ', ANSI.yellow));\n        if (res.exit) { console.log(color('Goodbye!', ANSI.green)); break; }\n        if (res.canceled) { continue; }\n        const cVal = res.value;\n        const fVal = celsiusToFahrenheit(cVal);\n        console.log(color(`${formatNumber(cVal)} °C = ${formatNumber(fVal)} °F`, ANSI.green));\n      } else if (c === '2') {\n        // Fahrenheit -> Celsius\n        const res = await askForNumber(rl, color('Enter temperature in Fahrenheit (e.g. 77): ', ANSI.yellow));\n        if (res.exit) { console.log(color('Goodbye!', ANSI.green)); break; }\n        if (res.canceled) { continue; }\n        const fVal = res.value;\n        const cVal = fahrenheitToCelsius(fVal);\n        console.log(color(`${formatNumber(fVal)} °F = ${formatNumber(cVal)} °C`, ANSI.green));\n      } else if (c === '3') {\n        console.log();\n        console.log(color('Formulas:', ANSI.bright));\n        console.log('  Celsius → Fahrenheit: (°C × 9/5) + 32 = °F');\n        console.log('  Fahrenheit → Celsius: (°F − 32) × 5/9 = °C');\n        console.log();\n        console.log(color('Examples:', ANSI.bright));\n        console.log('  0 °C = 32 °F');\n        console.log('  100 °C = 212 °F');\n        console.log('  32 °F = 0 °C');\n        console.log('  212 °F = 100 °C');\n      } else {\n        console.log(color('Invalid selection. Please enter 1, 2, 3 or 4.', ANSI.red));\n      }\n    }\n  } finally {\n    // Remove SIGINT listener and close readline\n    process.removeListener('SIGINT', sigintListener);\n    try { rl.close(); } catch (e) { /* ignore */ }\n  }\n}\n\n// If the file is run directly, start mainLoop\nif (process.argv[1] && process.argv[1].endsWith('temp-convert.js')) {\n  // Run main loop and catch unexpected errors\n  mainLoop().catch((err) => {\n    console.error(color('An unexpected error occurred: ' + (err && err.message ? err.message : String(err)), ANSI.red));\n    process.exit(1);\n  });\n}\n\n// Export mainLoop as default for potential programmatic use\nexport default mainLoop;\n"
    },
    {
      "path": "README.md",
      "content": "# TempConvert CLI\n\nTempConvert CLI is a lightweight Node.js command-line utility for converting temperatures between Celsius and Fahrenheit. It provides a simple interactive menu, input validation, and clean formatted output. No external dependencies — just a modern Node.js runtime (Node 17+/20+ recommended).\n\n## Features\n\n- Interactive menu-driven CLI\n- Convert Celsius → Fahrenheit and Fahrenheit → Celsius\n- Input validation (rejects non-finite values)\n- Helpful prompts and graceful handling of exit (Ctrl+C, Ctrl+D, `q`, `exit`)\n\n## Installation\n\nNo installation required besides Node.js. Save the repository files to a folder.\n\n## Usage\n\nRun the script with Node.js:\n\n  node temp-convert.js\n\nAlternatively, make the script executable (Unix) and run:\n\n  chmod +x temp-convert.js\n  ./temp-convert.js\n\nWhen running, you'll see a menu. Enter the option number and follow the prompts.\n\nAt any numeric prompt you can type:\n\n- `b` or `back` to go back to the main menu\n- `q`, `quit`, or `exit` to quit the application\n- Press Ctrl+C to exit immediately\n\n## Formulas\n\n- Celsius → Fahrenheit:\n\n  (°C × 9/5) + 32 = °F\n\n- Fahrenheit → Celsius:\n\n  (°F − 32) × 5/9 = °C\n\n## Examples\n\n- Convert 25 °C to °F:\n\n  25 °C = 77 °F\n\n- Convert 77 °F to °C:\n\n  77 °F ≈ 25 °C\n\n## Implementation Notes\n\n- Uses Node's built-in `readline/promises` for a promise-based prompt API.\n- Input validation uses `Number()` and `Number.isFinite()` to allow common numeric formats (including scientific notation) and reject NaN/Infinity.\n- Output numbers are formatted to a maximum of 4 decimal places, with trailing zeros removed for readability.\n\n## Run instructions\n\n1. Ensure you have Node.js 17+ (Node 20+ recommended).\n2. From the folder containing `temp-convert.js`, run:\n\n   node temp-convert.js\n\nEnjoy converting temperatures!\n"
    }
  ]
}